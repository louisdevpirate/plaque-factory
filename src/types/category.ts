export interface Category {
  slug: string
  title: string
  description: string
  image: string
  imagePlaque: string
  code: string
  features: string[]
  vehicleTypes: string[]
  dimensions: string
  icon: string
}